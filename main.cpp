#include "neural_network.h"
#include <iostream>



int main(int argc, char** args)
{   

    /*auto nn = neuralNetwork({1,3,3},sigmoid);
    Matrix m(1,1,0);
    m.randomize();
    nn.feedForward(m).print();*/

    std::cout<<power(2,-1);


    return 0;
}