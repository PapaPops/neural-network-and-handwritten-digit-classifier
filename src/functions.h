#include <math.h>

namespace Function
{
    float sigmoid(float);
    float tanh(float);
    float relu(float);
    float arctan(float);

    //derivatives

    float sigmoid_d(float);
    float tanh_d(float);
    float relu_d(float);
    float arctan_d(float);
}

